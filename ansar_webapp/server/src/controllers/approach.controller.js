// Example Approach controller
exports.getApproach = (req, res) => {
  res.json({ message: 'Approach endpoint' });
};

// Approach controller
exports.getApproachBySlug = (req, res) => {
  res.json({ message: 'Get approach by slug (stub)' });
};
